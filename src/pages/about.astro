---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About" description="Galaxy Kadiyala — Technical Program Manager focused on systems, execution, and leverage. 15+ years across cloud, infrastructure, and platform engineering.">
  <section class="section">
    <div class="container">

      <div class="about-grid">

        <!-- Left: big statement -->
        <div class="about-left">
          <h1 class="about-title">
            TPM.<br/>
            Systems builder.<br/>
            <em>Open web</em> believer.
          </h1>
          <p class="about-tagline">Most work fails at execution. I fix that.</p>
          <div class="about-accent-block">
            <span class="tag red">Technical Program Manager</span>
            <span class="tag blue">Systems Thinking</span>
            <span class="tag green">AI &amp; Infra</span>
            <span class="tag orange">2&times; US Patents</span>
            <span class="tag">Mozilla</span>
          </div>
        </div>

        <!-- Right: bio -->
        <div class="about-right">
          <div class="about-bio">
            <p>
              I work on systems that need to deliver, not just exist.
            </p>
            <p>
              Over the last 15+ years, I've worked across cloud, infrastructure, QA, and platform engineering. I focus on execution across distributed teams and complex dependencies.
            </p>
            <p>
              At Nutanix, Pure Storage, and Rubrik, I've worked on systems used by 10,000+ customers. This includes migrations, CI/CD transformations, and improving delivery predictability. I hold two US patents in distributed systems.
            </p>
            <p>
              My focus is simple. Turn messy execution into systems that scale.
            </p>
            <p>
              Outside work, I've been part of the Mozilla community since 2010. I mentor contributors and help scale open web initiatives across the world.
            </p>
            <p>
              That experience shaped how I think. Systems over effort. Leverage over activity.
            </p>
          </div>

          <div class="about-links">
            <a href="/projects" class="btn">View Work →</a>
            <a href="/contact" class="btn outline">Get in touch →</a>
            <a href="/resume" class="btn outline">Resume ↗</a>
          </div>
        </div>

      </div>

      <!-- Timeline -->
      <div class="timeline-section">
        <h2 class="timeline-heading">Timeline</h2>
        <div class="timeline">
          {[
            { year: '2010', label: 'Joined Mozilla community — Firefox evangelist', color: 'var(--accent-3)' },
            { year: '2011', label: 'M.Tech at IIIT Pune · Technical Lead at Terralogic', color: 'var(--accent-4)' },
            { year: '2013', label: 'Attended MozFest in London', color: 'var(--accent-2)' },
            { year: '2015', label: 'Mozilla Reps Mentor · FSA Bootcamp India', color: 'var(--accent-5)' },
            { year: '2017', label: 'Joined Nutanix as Senior Member of Technical Staff', color: 'var(--accent-1)' },
            { year: '2021', label: 'Engineering Manager / TPM at Nutanix', color: 'var(--accent-3)' },
            { year: '2022', label: 'US Patent — Verifying Snapshot Integrity', color: 'var(--accent-4)' },
            { year: '2023', label: 'US Patent — Managing HA File Servers', color: 'var(--accent-2)' },
            { year: '2024', label: 'TPM at Aziro — deputed to Pure Storage & Rubrik', color: 'var(--accent-5)' },
            { year: 'Now',  label: 'IIM PGCP grad · Reviving the blog. Still building.', color: 'var(--accent-1)' },
          ].map(item => (
            <div class="timeline-item">
              <div class="timeline-dot" style={`background: ${item.color}`}></div>
              <div class="timeline-year">{item.year}</div>
              <div class="timeline-label">{item.label}</div>
            </div>
          ))}
        </div>
      </div>

    </div>
  </section>
</BaseLayout>

<style>
.about-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 5rem;
  align-items: start;
  padding-bottom: 5rem;
  border-bottom: 1px solid var(--border);
  margin-bottom: 5rem;
}

.about-title {
  font-size: clamp(2.5rem, 5vw, 4.5rem);
  line-height: 1.05;
  letter-spacing: -0.03em;
  margin-bottom: 1.25rem;
}
.about-title em {
  font-family: var(--sans);
  font-style: normal;
  font-weight: 800;
  color: var(--accent-1);
}

.about-tagline {
  font-family: var(--mono);
  font-size: 0.82rem;
  color: var(--fg-2);
  margin-bottom: 1.75rem;
  letter-spacing: 0.01em;
}

.about-accent-block {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.about-bio {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
  margin-bottom: 2.5rem;
}
.about-bio p {
  font-size: 1.05rem;
  opacity: 0.8;
  line-height: 1.75;
}

.about-links {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
}

/* Timeline */
.timeline-heading {
  margin-bottom: 2.5rem;
  font-size: clamp(1.5rem, 3vw, 2.5rem);
}

.timeline {
  display: flex;
  flex-direction: column;
  gap: 0;
  position: relative;
  padding-left: 2rem;
}
.timeline::before {
  content: '';
  position: absolute;
  left: 7px;
  top: 10px;
  bottom: 10px;
  width: 2px;
  background: var(--border);
}

.timeline-item {
  display: grid;
  grid-template-columns: 80px 1fr;
  gap: 1rem;
  align-items: center;
  padding: 1.25rem 0;
  border-bottom: 1px solid var(--border);
  position: relative;
}

.timeline-dot {
  position: absolute;
  left: -2rem;
  width: 14px;
  height: 14px;
  border-radius: 50%;
  border: 2px solid var(--paper);
  outline: 2px solid var(--border);
  top: 50%;
  transform: translateY(-50%);
}

.timeline-year {
  font-family: var(--mono);
  font-weight: 500;
  font-size: 0.85rem;
  opacity: 0.45;
}

.timeline-label {
  font-family: var(--sans);
  font-weight: 600;
  font-size: 1rem;
}

@media (max-width: 768px) {
  .about-grid {
    grid-template-columns: 1fr;
    gap: 3rem;
  }
}
</style>
